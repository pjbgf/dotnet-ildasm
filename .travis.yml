language: csharp
dist: trusty

matrix:
  include:
    - dotnet: 1.0.1
      mono: none
    - mono: latest

before_install:
- chmod +x build.sh
- chmod +x run-cross-checks.sh
- dotnet restore

install:
- ./build.sh -c Release -f netcoreapp1.0

script:
- ./run-cross-checks.sh