language: csharp
dist: trusty

matrix:
  include:
    - dotnet: 1.0.1
      mono: none

before_install:
- chmod +x build.sh
- chmod +x run-cross-checks.sh
- dotnet restore

install:
- ./build -c Release

script:
- ./run-cross-checks.sh